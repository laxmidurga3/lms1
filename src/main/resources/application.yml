server:
  port: 8008
spring:
  application:
    name: usertype-service
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/laxmi_admin_db
    username: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate:
      ddl-auto: update
